name: cloudnative-bluegreen

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gym_management}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - gym-bluegreen-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-gym_management}"]
      interval: 5s
      timeout: 5s
      retries: 5

  reverse-proxy:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./proxy/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./proxy/conf.d:/etc/nginx/conf.d:ro
      - ./proxy/upstreams:/etc/nginx/upstreams
      - ./proxy/state:/var/lib/nginx-state
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - gym-bluegreen-network
    healthcheck:
      test: ["CMD-SHELL", "nginx -t"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  gym-bluegreen-network:
    external: true
    name: gym-bluegreen-network

volumes:
  postgres_data:
    external: true
    name: gym-postgres-data
